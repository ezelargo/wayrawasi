@model IEnumerable<WayraWasi.Models.Reserva>

<h2>Reservaciones</h2>

<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Crear Nueva Reservación</a>
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Nombre Cliente</th>
            <th>Fecha Entrada</th>
            <th>Fecha Salida</th>
            <th>Número de Personas</th>
            <th>Cabaña</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var reservacion in Model)
        {
            <tr>
                <td>@reservacion.NombreCliente</td>
                <td>@reservacion.FechaEntrada.ToString()</td>
                <td>@reservacion.FechaSalida.ToString()</td>
                <td>@reservacion.NumeroPersonas</td>
                <td>@reservacion.IdCabania</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = reservacion.IdReservacion })" class="btn btn-warning">Editar</a>
                    <a href="@Url.Action("Details", new { id = reservacion.IdReservacion })" class="btn btn-info">Detalles</a>
                    <a href="@Url.Action("Delete", new { id = reservacion.IdReservacion })" class="btn btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>